%h2= link_to @company.name, @company_authenticated
%h3= @company.email
%p= link_to 'I have a new bar', new_company_bar_path
%ol
  - @company.bars.each do |bar|
    %li= link_to bar.name, company_bar_path(bar)

/ This if statement allows only for the admin user to have access to editing and deleting bar
-if @user_authenticated.is_admin?
  %li= link_to 'Edit Bar', edit_bar_path
  %li= link_to 'Destroy Bar', bar_path, :method => 'delete', :confirm => 'Are you sure?'